

export interface ICardButton {
    img: string;
    text: string;
    count: number;
}

export interface ICategory {
    id: number;
    img: string;
    text: string;
    count: number;
}

export interface IBrend {
    id: number,
    name: string,
    img: string
}